# @zynkreatech packages are fetched from / published to GitHub Packages,
# not the public npm registry. The scope MUST equal the GitHub org name.
@zynkreatech:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GH_PACKAGES_TOKEN}

# Peer dependency friction reducers — safe defaults for a workspace this size.
auto-install-peers=true
strict-peer-dependencies=false