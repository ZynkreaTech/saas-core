# @zynkreatech/module-custom-crm

Customer Relationship Management module. See `manifest.json` for platform
metadata (version, dependencies, category). This module depends on
`organization` and `users` being installed first — the platform installer
enforces this before running `install()`.